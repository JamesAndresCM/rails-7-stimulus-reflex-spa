<!DOCTYPE html>
<html>
  <head>
    <title>Tasker</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <%= render "partials/nav" %>
    <div class="container">
      <div class="my-4 d-flex justify-content-between align-items-center">
        <h4>Hello: <%= Current.user.name %></h4>
        <h4>Team: <%= current_user.team.name %></h4>
      </div>
      <%= yield %>
    </div>
  </body>
</html>
